# 🧠 Lecture 3: Regular Expressions (Part 1)

## 📌 Overview
Regular Expressions (Regex) are a powerful tool used in Natural Language Processing (NLP) and programming for **pattern matching**, **text searching**, and **data validation**.

They provide a formal and compact way to describe sets of strings (languages).

---

## 🔍 What is a Regular Expression?

A **Regular Expression (Regex)** is:

- A pattern used to match text
- A sequence of characters + special operators
- A formal way to define a **language (set of strings)**

### ✅ Key Uses:
- Text searching 🔎
- Data validation (emails, phone numbers) ✔️
- Text replacement ✏️
- Parsing structured text 📄

### 💻 Supported In:
- Python (`re` module)
- JavaScript
- Java
- C++
- Ruby
- C#

---

## 🌍 Regex and Languages

- A **language** = set of strings
- A **Regex** defines a language

### Definitions:

| Concept | Meaning |
|--------|--------|
| Σ (Alphabet) | Set of symbols |
| String | Sequence of symbols |
| L(r) | Language generated by regex r |

---

## 🧩 Basic Regular Expressions

| Expression | Meaning |
|-----------|--------|
| `a` | Matches character "a" |
| `ε` | Empty string |
| `∅` | Matches nothing |

---

## 🔗 Concatenation

Combining characters in sequence.

```regex
/wood/
